plugins {
    id 'com.android.application'
}

android {
    namespace 'com.example.bluetoothkeyboardapp'
    compileSdk 33

    defaultConfig {
        applicationId "com.example.bluetoothkeyboardapp"
        minSdk 28
        targetSdk 33
        versionCode 1
        versionName "1.0"
    }

    buildTypes {
        release {
            minifyEnabled false
            proguardFiles getDefaultProguardFile('proguard-android-optimize.txt'), 'proguard-rules.pro'
        }
    }
}

dependencies {
}
